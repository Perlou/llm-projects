## 🚀 快速开始

### 1. 环境设置

```bash
# 配置 API Keys
cp .env.example .env
# 编辑 .env 填入你的 API Keys

# 进入某个具体项目
cd ./demo-project

# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

```
